import React from 'react'

function List(props) {
  return (
    <div>
        {
            console.log(items)
            // props.items.map(item => {
            //     <div key={item.id}>{item.name}</div>
            // })
        }
    </div>
  )
}

export default List
